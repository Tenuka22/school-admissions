import type { MarkingSubversionDelta } from "../../../shared/types";

/**
 * Initial (and currently only) marking subversion — the complete G1 2027
 * scheme: all six circular categories (6.1–6.6), every scored field, step
 * groupings and the map-proximity deduction fields.
 */
export const subversion1Delta: MarkingSubversionDelta = {
  added: [],
};
